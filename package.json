{
  "name": "extract",
  "version": "1.0.0",
  "description": "Extract just the content from a web page.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@jocmp/mercury-parser": "^3.1.0",
    "express": "^5.2.1",
    "yaml": "^2.9.0"
  }
}
